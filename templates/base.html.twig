<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Sortir ENI{% endblock %}</title>


    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">

    <link rel="stylesheet" href="{{ asset('css/app.css') }}">

</head>
<body>
<div class="entete position-relative d-flex align-items-center mb-4" style="height: 120px;">
    <img src="{{ asset('images/sortir.svg') }}" alt="Sortir" style="width:150px; height:150px;">
    <h1 class="ms-3" style="color: #006400;">SORTIES AVEC L'ENI</h1>
    <nav style="position: absolute; right: 0; bottom: 0; margin-right: 20px;  display: flex; gap: 50px;">
        <a href="{{ path('sortie_list') }}">Toutes les sorties</a>
        <a href="{{ path('sortie_create') }}">Créer une sortie</a>
        <a href="{{ path('modify_profil') }}">Modifier mon profil</a>
        <a href="{{ path('app_login') }}">Login</a>
    </nav>
</div>

{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="alert alert-{{ label }} alert-dismissible fade show">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}


<div class="container">
    {% block body %}{% endblock %}
</div>


</body>

</html>
